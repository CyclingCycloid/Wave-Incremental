# Changelog

## v0.5.0.2 — The Virtual Particles Update

### 新增虚粒子升级（SVPU4/5）

- **SVPU4 热能超载**（1e7 VP 起，每级 ×1000）：削弱温度的软上限——8DA 打破规则后，
  超出普朗克温度部分的缩放指数由 (lg(Tp)/lg(T))^(1/2)/2 变为 (lg(Tp)/lg(T))^(1/(n+2))/2
  （n 为等级，n=0 与原公式一致）
- **SVPU5 潮汐撕裂**（5e7 VP 起，每级 ×2000）：黑洞质量的软上限起始点每级 +10 个数量级——
  1e50 → 1e(10n+50)，公式同步变为 1e(10n+50)×(Gain/1e(10n+50))^((15/lg(Gain))^(1/2))；
  黑洞页软上限提示与帮助页同步为动态起始点；SVPU4/5 布局位于「虚粒子升级」标题下居中首行，按钮尺寸与 SVPU1/2/3 一致

### VPU4 实装：对偶原理

- 全息原理（SVPU1）的等级上限 4 → 6（购买/满级判定/显示同步）
- 解锁条件：黑洞质量达到 1e70 太阳质量（未达成时卡片显示条件与当前质量），价格 2e8 VP

### 新增成就

- **A51 虚幻**：购买第一个虚粒子单次升级（第 5 行）；购买 VPU 时检查

### 界面与调整

- SVPU4（热能超载）与 SVPU5（潮汐撕裂）改为购买 VPU5 后作为其奖励出现
  （VPU5 描述追加「增加两个虚粒子升级」）；SVPU1/3/4 描述删除括号内的上限说明
  （上限仍显示在按钮的「x/n」进度中）
- 虚粒子吸积衰减改为分段：VP<1e10 每秒 ×9/10；VP>1e10 每秒 ÷(lg(VP)/9)
  （lg=10 处连续，VP 越大消耗越快）；帮助页同步
- VPU 解锁条件只需达成一次：达成后永久记录（state.vpuCondMet，随档保存），之后即使条件
  回落（如黑洞质量被脉冲消耗到 1e70 以下、挑战时间变慢）仍保持开放；
  仅当未来出现比湮灭更高层次的重置时才会清除
- 奇点页 AU 按钮行改为按行交错排列：AU2n 与 AU1n 同行对齐（原为两列独立流，内容高度
  不同导致横向错位）
- S25「这是旮旯给木吗？」条件改为好感度达到 500（原 1000）
- 黑洞动画大小公式改为 max(0.1, min(0.6, min((lg(M)/800)^0.5, lg(M)/200))) 倍页面宽：
  线性段 lg/200 与平方根段 (lg/800)^0.5 取较小者，大质量时增长更缓
  （lg(M)≥288 封顶 0.6 倍；原公式为 lg(M)/50 线性）

## v0.5.0.1 — 数值安全与文案修正

### 严重修复（终局存档污染 / 资源清零 / 免费购买）

- 修复 timeRateLog 黑洞扭曲加成把乘法算成加法：×(1+效果) 误写为 logAddLogs(总log, 效果log)，
  效果巨大时吞掉成就/时间之矢/绝对零度的全部贡献，且与显示的时间速率不一致；
  改为 log + log10(1+10^效果)
- 修复 subULog 只防「U 超 double」未防「价格超 double」：价格 >1e308 时
  Math.pow(10,价格log)·L = Infinity，U 被 -Infinity 清零且未实际付款；补价格 log 检查
- Sp/声子/VP 的加减全部接入 log 域（新增 addSpLog/subSpLog/addTotalSpLog/subPhononsLog/subVPLog）：
  此前 sp 缓存为 Infinity 时，湮灭结算与 SAU/AU/SBU/SVPU/VPU/CD/批量/声子升级扣款
  会把权威 log 写成 LOG_CAP（下次加载触发污染全清）或 NaN（资源清零 + 后续购买全部免费）；
  可负担判断与按钮显示统一走 cmpGE/spAfford
- 修复 tick 声子生产缺溢出防护：pr·dt 或缓存 Infinity（含 trLog>308 时 dt=MAX_VALUE 占位）
  会 setPhonons(Infinity) 污染存档；补与 U 路径同款的「和有限性」检查
- 修复 dt 溢出窗口：trLog 接近 308 且 realDt 较大（后台标签节流）时 dt=Infinity，
  gameDtLog 经 clampLog 变 LOG_CAP 污染存档；dt 非有限时改走 log 域（gameDtLog 统一为权威）
- vpSpMult（AU42）与 spAccretionMult（AU43）补 log 域版本：VP/totalSp 缓存 Infinity 时
  不再向 Sp 获取链/黑洞吸积速率注入 Infinity
- realDt 钳制 [0, 60s]：挂起标签的一次性补发上限 60 秒，时钟回拨不再产生负 dt 倒扣资源
- 修复获取速率为 0 时显示成 1.000e-999999xxx 的问题：零哨兵 NLOG 上直接累加
  timeRateLog 等修正产生哨兵噪声（-1e9+18.9），被显示层误判为 double 下溢值；
  新增 gainRateDispLog 口径（哨兵保持语义零），修正 up1 卡「当前获取速率」与全局栏「Hz/s」两处

### 新功能：离线进度系统

- 重新打开页面时自动结算离开期间的生产（波速/声子/黑洞吸积脉冲）与自动化
  （自动购买/自动湮灭，含扭曲宇宙达标自动完成），时长上限 8 小时，≥60 秒起算
- 实现为粗步长虚拟时钟模拟：生产块从 tick 抽取为 applyProduction 供在线/离线共用，
  膨胀波长/冷却指数/rua 倍率等墙钟公式改经 gameNow() 虚拟时钟（在线行为逐位不变）
- 模拟期间 UI 与存档写入全部挂起（simActive 守卫 21 处），结束后「欢迎回来」弹窗
  展示收益（波速/声子/黑洞质量/虚粒子/湮灭次数/奇点）并立即入账；
  黑洞质量/虚粒子的衰减以 ÷ 倍数表述（衰减为乘法）
- 设置页新增「离线收益」开关（默认开启，随档保存），帮助页补充说明

### 新功能：成就系统改版

- 可见性：一行解锁后即显示该行全部成就的名字与星标，带奖励的成就可点击查看（无需完成）；
  未解锁的行默认整体隐藏（不再显示 ??? 占位行）
- 成就页时间倍率提示下方新增两个显示选项：「隐藏不可见的成就行」（默认开，取消后以 ???
  占位显示）与「隐藏已完成的成就行」（默认关），勾选状态随存档保存
- 修复首次湮灭获得 2 Sp 的问题：首湮灭保底 max(1, 基础值) 误实现为 1+基础值
  （普朗克温度处基础值恰为 1 → 得 2）；现首次严格 1 Sp。第二次起公式不变：
  1.42e32→1e50 K 线性 1→10；1e50→1e100 K 为 lg(T)/5；≥1e100 K 为 2·T^0.01（三段连续）

### 平衡与调整

- AU14 黑体辐射：价格 5e16 → 3e16，效果 ×max(1, L^-0.05) → ×max(1, L^-0.1)
- VPU 虚粒子单次升级区在达成 A45「万物」前整区不可见（此前对已解锁黑洞的玩家显示占位九宫格）；
  未达成解锁条件的 VPU 卡片显示具体达成条件（含当前进度）
- VPU5 临界湮灭：定价 1e7 虚粒子（条件：所有扭曲宇宙最佳完成时间之和 < 3 秒）；
  VPU 购买货币由 Sp 改为 VP，与「虚粒子单次升级」定位一致
- 滞涨宇宙描述更新：「价格折算即刻生效且变得更强，波速获取和温度开平方根」（仅文案，机制不变）

### 一般修复

- 修复扭曲页「退出」按钮一次点击计 2 次失败（exitDistortBtn 与 exitDistort 各 +1），
  S14 五次点击即可达成；现统一在 exitDistort 计数，与顶部湮灭按钮路径一致
- 修复 buyUp3 的 minL 更新分支方向/符号错误（新波长 log 为 -wLog），此前为永不触发的死分支
- 修复 Sp=0 显示为 1（fmtNum 双零走下溢分支显示 1.000e0）
- 修复 S19「滚木」用 state.L>0 判零产出：波长 double 下溢为 0 但 log 域生产仍为正时虚假达成；改 log 域判定
- 自动化输入框拒绝 Infinity（如 1e999）与负数（parseSciInput 返回 NaN 保持原值）
- migrateState 补 sp/totalSp/playTime 的 null 回填（playTime=Infinity 序列化为 null 后不再显示 NaN）
- saveToSlot 同步 currentSlot：保存到槽后「当前」高亮不再在刷新后错位

### 文案修正

- 帮助页 AU42 虚幻凝聚价格 5e12 → 5e9（与实际一致）；AU41/AU42 描述「奇点效果」→「奇点获取」
- 购买提示旧名「频率加成波长获取」→「频率加成波速获取」
- 统计标签「时间速率（普通成就加成）」→「时间速率」（值包含黑洞/A41/rua 等全部来源）
- 统计「当前宇宙温度上限」在扭曲宇宙内改显该宇宙的目标温度（effectiveCapLog）
- 升级 2 / 声子倍率卡片效果显示改用真实公式（含 AU13 底数加成与 AU12 免费等级）
- A35 检查补真空衰变；A25 描述改为「达到普朗克温度(1.417e32K)」；A12 检查补「解锁声子」
- 黑洞软上限提示措辞改为「质量获取超过 1e50 的部分」（与帮助页一致）
- 统计占位成就数 0/5、0/9 → 0/20、0/25；湮灭按钮半角括号与缺空格修正；「e100」→「1e100」
- 帮助页补：扭曲首杀 ×2 仅首次、各宇宙规则细则、升级 3 的 AU23 例外、温度上限 1e250 软上限、
  SAU1/3 与 SVPU 上限、VPU5 解锁条件、A42 解锁的 CD 升级、黑洞 SVPU 段落
- 滞涨宇宙价格描述精确化（升级 1 为 100^n、升级 2 自 1e6 起每级 ×max(n²,100)）
- fmtLog 的小数位数跟随设置（原硬编码 3 位）；GitHub 链接改独立 repo-link 类名（不再复用 changelog-link）
- CHANGELOG v0.5.0 与代码不符处以代码为准修正（滞涨目标 1e110、AU43 ^3、黑洞软上限 15/lg、
  AU44 移动的是 SBU1 事件视界、通胀 ^0.75、SBU2 +0.05/级、25ms 分度）；游戏内多处过时注释同步

### 其他

- 死代码清理：setL、logAdd、up3Wavelength、up3WavelengthLog、softcapExtraLog、
  空数组 SP_UPGRADES 及其构建/购买/更新分支、baseSpGain（Sp 公式 double 版，唯一实现为
  log 域 spGainBaseLog）、index.html 死属性 data-stats-subtab
- 文件头版本注释 v0.4.2.4 → v0.5.0

## v0.5.0 — The Distort Update

### 8DA：打破多元宇宙规则（实装）

- 奇点页顶部居中出现「打破多元宇宙的规则」按钮（红色），按下后变金色「恢复多元宇宙的规则」
- 打破规则（仅主宇宙生效，扭曲宇宙仍是硬上限）：
  - 普朗克温度从硬上限变为软上限——超过 Tp 的部分按 (lg(Tp)/lg(T))^(1/2)/2 次方缩放
  - 声子页温度区显示红色提示「超过普朗克温度的部分将受到软上限限制」
  - 隐藏奇点面板「多元宇宙的规则正在阻止你获取更高的温度」警告
- A43「无限」成就可正常达成

### 数值安全（log 域化）

- 游戏时间倍率引入 timeRateLog（log 域权威）：黑洞扭曲状态等把倍率推过 double 时，
  游戏时间以 log 累积、double 缓存封顶 MAX_VALUE，不再产生 Infinity
- Sp 获取链（spGain/spGainExact/spGainLog）统一 log 域：温度超 double 时不再经
  Decimal.pow(Infinity) 产生 NaN/Infinity，湮灭结算不再损坏存档
- 湮灭 Sp 结算在 gained 超 double 时走 log 域累积（logDsp/logDtotal 权威）

### 平衡调整

- 滞涨宇宙目标温度保持 1e110 K
- AU34 引力扭曲：扭曲状态效果 ^1.5 → **^2**
- AU43 奇点塌缩（7DA，5e12 Sp）：新增黑洞吸积效率倍率 (lg(Sp+1)+(Sp+1)^0.01)^3；
  修复了 AU43 从未实际作用于吸积速率的 bug
- AU44 **监察原理**（打破多元宇宙规则解锁，1e14 Sp）：SBU1 事件视界的加成在软上限后生效
- 黑洞质量获取超 1e50 部分受软上限：实际获得 = 1e50×(Gain/1e50)^((15/lg(Gain))^(1/2))，
  黑洞界面显示黄色提示「由于黑洞质量溢出，超过1e50的部分将受到软上限影响」
- SVPU1 全息原理：上限 6 级 → **4 级**，价格改为每级 ×100（10/1e3/1e5/1e7 VP）
- SVPU2 虚幻湮灭：效果从加成总湮灭次数改为**每次获得的湮灭次数 ×2^n**，价格 3×5^(n-1) VP
- SVPU3 非欧几何：价格增速 ×1e3 → ×1e5
- 自动湮灭 CD 缩减升级：**上限 3 级**（CD 25ms 触底），满级后拒绝购买
- 批量购买升级：打破规则且上限超 128（最大购买状态）后不可购买
- 自动化时间/奇点输入栏以 AeB 字符串形式显示大数值

### 新增成就

- A45「万物」：购买所有奇点升级

### 其他

- 删除全部测试按钮（危险操作区仅保留硬重置）
- 8DA 里程碑 7DA 前显示 ？？？？？，之后金色文字；完成后整条金色
- 挑战/湮灭时间显示分度 25ms
- 修复 AU24 声子保留在进出扭曲宇宙时未失效的 bug
- 修复扭曲页面退出/重试错误记录挑战时长（显示已湮灭）的 bug
- 存档槽预览频率改 log 域计算（超 double 显示 1eN 而非 ∞）
- 手机端成就布局：逻辑行拆 3+2 居中，不与其他行混排
- 设置页新增 GitHub 仓库链接与 rua 摆线功能（好感度/随机倍率加成）

## v0.4.3 — The Black Hole Update

### 修复：>1e308 真正完整接入

- 根因修复：购买扣款在 `state.U = Infinity` 时调用 `setU(Infinity - cost)` 把 logU10 权威压成 308，摧毁 log 表示。新增 `subULog`（log 域减法），购买仅在 U 在范围内时走 double，否则走 log 域；`setU` 收到非有限正数时保留原 logU10 不动。
- `gainRate()` 的 meta1 因子（`1 + lg(F+1)`）改用 `FLog()`，不再被 `F()=Infinity` 污染；`gainRateLog` 的 meta1 分支同理修复（原先 `Math.pow(10, lf)` 溢出）。
- 验证：`setULog(400)` 后购买升级，`getLogU10()` 不再塌成 308；F 持续增长越过 1e308。

### 扭曲宇宙调整

- 膨胀宇宙：波长每秒 ×1e20（原 1e15）；新增获取波速指数随时间下降（每秒 -0.1，到 0 为止）；目标温度 1e300 K。
- 简洁宇宙：普朗克常数倍率始终为 1；第一个奇点效果平方根（奇点加成指数 ^2 → ^1，即 ÷2）。
- 通胀宇宙：频率获取由平方根（^0.5）削弱为 ^0.75。（注：v0.5.0 该宇宙重设计为「滞涨」，波速获取为平方根 ^0.5）

### 黑洞系统（5DA 里程碑解锁，黑色）

- 湮灭标签下新增「黑洞」子页（5DA 解锁，黑色主题，5DA 里程碑标记黑色）。
- 黑洞质量 M（初始 1 太阳质量）、虚粒子 VP；基础效果 M^0.2 给予时间倍率加成（扭曲状态）。
- 三个状态（手动切换，无冷却）：
  - 吸积：质量获取 `M^0.75 × (F/1e200)^0.01`，无加成，虚粒子每秒衰减为 9/10。
  - 扭曲：给予时间倍率加成 `×(1 + bhEffect)`，无质量变化，无虚粒子。
  - 脉冲：每秒质量 ×0.5（损失一半，到 1 为止），获取 `floor(M^0.1)` 虚粒子/秒（M=1 停止）。
- 三个 SBU 升级（黑色按钮，花 Sp）：
  - SBU1 事件视界：吸积效率 ×2/级，价 1e9·100^(n-1)。
  - SBU2 引力潮汐：黑洞效果 ×2/级，价 1e10·1000^(n-1)。（注：现为效果指数 +0.05/级）
  - SBU3 霍金辐射：虚粒子获取 ×2/级，价 1e11·100^(n-1)。
- SAU3 改名「紫外灾难」（原「霍金辐射」，热涨落指数效果不变）。
- 旋转黑洞动画（canvas + rAF）：吸积盘环 + 粒子；半径随 `1+lg(M)` 增大，初始 0.1 倍页面宽，上限 0.6 倍。三状态视觉区分：吸积粒子向内螺旋、扭曲加速旋转、脉冲中心变红向外吐粒子。

## v0.4.2.5 补充：break_infinity 完整接入（log 域全量扩展）

- 资源双表示扩展到 U / 累计频率 / 统计极值 / 升级3峰值：logU10、logTotalF、logMaxF、logMaxU、logMinL、logUp3LastF 全部成为权威 log10 表示，对应 double 字段为缓存。
- log 域公式链补全：planckMultLog、temperatureCapLog、temperatureLog、thermalMultLog、fluctMultLog、couplingMultLog、invLMultLog、phononRateLog、gainRateLog、spGainLog。
- 价格 log getter：up1CostLog/up2CostLog/pg1CostLog/pg2CostLog/pg3CostLog + costOfLog。
- 比较 helper cmpGE/cmpLT：double 范围内逐位不变（零回归），仅在双方饱和（≥1e290 或非有限）时退化为 log 域比较——修复 Infinity vs Infinity 死锁（升级3峰值超 1e308 后永久不可购买 / 自动化失效）。
- tick 累积（U / 累计频率 / 声子）在 double 饱和时自动切换 log 域累积（logAddLogs / logAddSigned），数值可越过 1e308 继续增长到 1e400、1e600…
- 升级3 历史峰值改用 logUp3LastF 权威存储，取代旧版 Infinity 哨兵。
- 显示层接入 fmtLog：U / F / 获取/Hz/s / 温度 / 累计频率 / 最高频率·波速 / 最低波长 / sp / 总sp / 普朗克倍率 / 温度上限 / 各价格 / 升级3峰值 / 扭曲宇宙目标温度 在超 1e308 时显示为 1eN（而非 ∞）；下溢侧波长不再丢精度。
- 修复统计 maxU 恒为 Infinity、minL 下溢为 0 丢精度的问题（log 域 max/min）。
- 旧存档 migrateState 自动回填全部新 log 字段。

## 已知限制（待下版本处理）

- 完整 Decimal 对象化重构仍为待办：当前 log 域表示在比较/累积/显示上覆盖了所有 >1e308 / <1e-308 场景，但若需直接对超 double 数值做减法/取模等非线性运算，仍需 Decimal。

## v0.4.2.4 补充：break_infinity 接入（3DA）

- 资源双表示：sp/totalSp/phonons 增加 log10 权威表示（logDsp/logDtotal/logDph），所有写入走 setter 同步，double 字段作为缓存。
- 温度链全程 log 域计算：phonons 用权威 log 表示，超 1e308 不再溢出（Infinity 承接 + Decimal 上限计算）。
- 1DA 效果（planck/cap/gainRate）在 totalSp 超 double 时用 Decimal 计算指数。
- 显示层：fmtLog 以 eN 格式显示超 double 数值；F 超 double 时频率大字直接显示 eN。
- 危险操作新增「测试：重置数值」按钮（奇点→1e5、累计→4e6、频率→100）。

## v0.4.2.4（测试版本）

- 3DA 里程碑实装：奇点升级系统（位于奇点页，spu1 下方）。
- 可重复类（SAU）：象限拓张（声子升级3上限 +2/级）、奇点凝聚（奇点效果 ×1.1/级）、霍金辐射（热涨落指数 +0.005/级）。
- 单次类（AU，四组）：机械共振、受激跃迁、光子共振、黑体辐射、时序扩张、末日时钟、声纹记忆、量子涟漪、时间之矢、成就刻印、绝对零度、引力扭曲（WIP）与占位组。
- 批量购买上限升级移至自动化页；A31 星标与奖励移除（与 A25 重复）。
- 全局栏：奇点下方显示当前游戏速率；频率前加「你的波动的频率为：」小字。
- 扭曲页：重试/退出按钮（均不完成挑战；达标时使用触发 S18）。
- 成就：A35 刻写（购买第一个奇点升级）、A41 视界、A42 烂柯、A43 无限（原 A35）、A44 永炽、A45 占位；S16 硬核玩家、S17 禅、S18 getting over it、S19 滚木、S20 version control。
- Sp 获取连续化：distortMult 乘在 floor 内部，baseSpGain 各分支返回连续值。

## v0.4.2.3（测试版本）

- 膨胀宇宙加强：进入 1 秒后每秒波长膨胀 1e15 倍。
- 修复膨胀宇宙波长/F 值在倍率超 double 时变 Infinity 的问题（log 域计算，波长显示改为指数形式）。
- 新增隐藏成就 S15「这是距离增量吗？」（有效波长达到 1e308 m）。
- 批量购买上限升级价格增速改为 ×5。
- 设置新增「界面刷新频率」三档 + 数值显示插值（消除 100ms 阶跃感）。

- 统计新增「挑战」选项卡：记录各扭曲宇宙的最佳完成时间，以及所有挑战时间之和（20 湮灭后可见）。
- 设置页新增通往 Changelog 的链接。
- 扭曲页汇总行居中放大（数字 21px）；各扭曲宇宙描述去除括号补充说明；膨胀描述改为"波长随时间指数增长"。
- Sp 获取公式重做：T < 1e50 K 不变；1e50 ≤ T < 1e100 K 为 floor(lg(T)/5)（线性到 20）；T ≥ 1e100 K 为 floor(2·T^0.01)。
- 修复批量购买按钮不显示的问题（className 覆盖 hidden 类）。
- 设置新增「界面刷新频率」三档（流畅 60fps / 平衡 30fps / 省电 10fps）：逻辑节奏不变，快变数字（频率/获取/温度等）按所选频率平滑刷新。
- 成就奖励系统：A25/A31 奖励"各个重置后波速为 100 m/s"、A34 奖励"解锁批量购买"；带星成就描述去除括号奖励文本，改为点击单元格显示小黑框（格式"Amn//奖励文本"）；成就页新增提示行。

## v0.4.2.2（测试版本）

- e100 软上限：频率超过 1e100 Hz 后，升级 1 价格增速 ×10（每级 ×10）、升级 2 价格增速变为乘当前等级、升级 3 效果的超出部分按 5/√(log₁₀F) 缩放指数；主要页底部显示软上限提示。
- 设置页新增测试按钮：临时打破宇宙规则（取消温度上限，保留全部进度，期间湮灭不获得 Sp；v0.4.3 移除）。
- 引入 break_infinity.js：1DA 后奇点效果（三次方）与温度链使用 Decimal 计算，数值不再受 1.8e308 限制。
- 绝热宇宙：同时禁购声子升级 3（指数加成）。
- 冷却宇宙：全局资源栏下方小白字实时显示当前指数 k。
- 湮灭按钮位置描述由"右上角"改为"正上方"。
- A25 奖励：每次重置（湮灭/升级3/退出扭曲）后初始波速 100 m/s（解决通胀宇宙起步问题）。
- A34 奖励：解锁批量购买——前两个自动化右侧新增模式按钮（单次购买·蓝 / 批量购买·紫 / 最大购买·金）；批量上限初始 2，可用奇点升级翻倍（初始 1 Sp，增速 ×2）；打破规则且上限超过 128 后取消限制并变为"最大购买"。
- 扭曲里程碑（按已湮灭扭曲宇宙数 DA，暗红色）：1DA 所有奇点效果三次方；3DA 更多奇点升级（WIP）；5DA 黑洞（WIP）；8DA 打破宇宙规则/取消温度上限（WIP）。

## v0.4.2.1（测试版本）

- 扭曲阶段测试：v0.4.2.X 为扭曲阶段测试版本，实装时更新到 v0.4.3。
- 界面：湮灭标签移至波动与统计之间；自动化与设置互换位置。湮灭按钮放大并置于标题/version 下方居中。
- 帮助：Sp 获取公式描述更新（T<1e50K 随指数线性增至 10，之后 floor(10·(T/1e50)^0.024)）；扭曲章节改为测试版描述。
- 统计：重置子页新增最近十次湮灭记录（时间/Sp 获取量/Sp每分，扭曲湮灭红框标记）；数据页湮灭栏新增扭曲宇宙湮灭数。
- 扭曲系统（测试）：第 20 次湮灭解锁，四个测试宇宙——刚性（升级3效果四次方根、禁购声子升级3）、膨胀（进入 1 秒后波长获得 2^((t-1)/2) 倍率）、定向（每刻 50% 波速获取取反、F 取绝对值、声波耦合失效）、简洁（基础获取固定 1、升级 1/2 无效、升级 3 指数 ×1.5）。
- 扭曲规则：进入扭曲宇宙立即湮灭重置；达到该宇宙普朗克温度后湮灭它（首杀 Sp 获取 ×2），未达标点击湮灭按钮则退出；扭曲内湮灭不获 Sp；自动湮灭在扭曲内于达标瞬间自动执行。各宇宙温度与奖励为测试值待调。
- 成就实装：A34 秩序（湮灭扭曲宇宙）、S14（扭曲宇宙失败十次）。

## v0.4.2

- 帮助页实装：按 CHANGELOG 顺序讲解机制（波与频率 / 声子与温度 / 湮灭与奇点 / 扭曲 / 通用），章节带版本标签与展开按钮。
- 帮助章节随进度开放：解锁声子、首次湮灭、第 20 次湮灭分别对应三章出现。

## v0.4.1

- 修复：首次湮灭遮罩文字改为白色（黑屏背景下可读）。
- 奇点页新增总奇点加成面板：波速获取倍率、普朗克常数倍率、当前宇宙普朗克温度。
- 主标签页点击时切换到默认子页：波动-主要、统计-数据、湮灭-奇点。

## v0.4.0 — The Annihilation Update

- 新增「湮灭」重置层：首次在普朗克温度（1.4168e32 K）达成后触发全屏事件（"现在的宇宙无法承载你的波动……"），播放黑屏动画后进行第一层大重置。
- 奇点（Sp）：显示于全局资源栏频率下方（紫色）。基础获取：10 Sp 以下随温度指数线性（1 Sp @ T_P0，10 Sp @ 1e50 K），之后 10·(T/1e50)^0.024，floor 取整。
- 宇宙常数：总奇点 Sp 使波速获取 ×(1+Sp)²、普朗克常数 ×(1+Sp)^1.5（等价温度倍率）、温度硬上限 T_P = t·(1+Sp)^10。
- 「湮灭」主选项卡（紫）：里程碑（1/2/3/5/8/10/20 次湮灭，含自动升级3、自动湮灭、扭曲解锁）与奇点升级（首个：除升级3外升级不再消耗资源，1 Sp）；「扭曲」子页占位。
- 「自动化」主选项卡：主要页升级（1e10 Hz）、声子页升级（1e20 Hz）自动购买；第 8 次湮灭解锁自动升级3（可设倍率）；第 10 次解锁自动湮灭（可设 Sp 阈值）。5 湮灭前湮灭重置开关状态。
- 右上角「湮灭」按钮（所有页面可见，达到当前宇宙上限时出现，显示本次可获 Sp）。
- 统计页分为「数据/重置」子页；数据分通用（黑）与湮灭（紫）两区，湮灭统计：本次耗时（真实/游戏）、总奇点、最好单次 Sp、最好 Sp/分、最快湮灭、当前宇宙温度上限。
- 新增成就：A31 创生、A32 Qol、A33 扭曲、A34 秩序（待扭曲实装）、A35 无限（待扭曲实装）；S10 歪了、S11 踌躇不决、S12 就你特殊？？！！、S13（升级3自动化填 <1）、S14（待扭曲实装）。

## v0.3.0

- 频率与每秒获取移至全局资源栏：任意页面均可见（主标签页上方）。
- 平衡调整：单次升级"频率加成波速获取"效果改为 ×(1 + lg(F+1))。
- 新增声子系统（1e10 Hz 解锁，波动标签下新增"声子"子页面）：
  - 声子发生器：启动后每游戏秒产生声子；声子数浮点存储、整数显示。
  - 温度 T = n·h·F/k_B（开尔文）；热涨落使波速获取 ×max(1,T)^0.2。
  - 发生器升级（可重复）：效率（间隔 ÷1.5/级，花 F，×100 增速）、倍率（第 n 级 ×(n+1)²，花 P，×2 增速）、升级3指数加成（+0.01/级，上限 20 级，花 P，×10 增速）。
  - 单次升级：声子涨落（1000 P，温度加成声子获取）、声波耦合（10000 P，波速加成声子获取）。
- 普通成就第二行：A21 热学（启动声子发生器，奖励 up1 效果 1.5 次方，★）、A22 室温、A23 耦合、A24 聚变、A25 湮灭。拥有特殊奖励的成就右上角标 ★。
- 新增隐藏成就：S7 请注意使用规范（10 秒内开关声子发生器 20 次）、S8 无用功（加成 <1.1× 时购买升级3）、S9 柚子厨蒸鹅心（导入框输入 0721 并导入）。

## v0.2.1

- 平衡调整：单次升级"频率加成波长获取"正式定价 5000 Hz，由"未开放"改为可购买。
- 成就位置轮换：A12 协同、A13 超声、A14 效率（原 A12 超声、A13 效率、A14 协同）。
- 隐藏成就 S3"快点端上来罢"条件更新：在价格不足以购买单次升级时，5 秒内点击购买按钮 10 次。单次升级购买按钮永不禁用，以支持该成就；价格充足时点击即购买。后续新增的单次升级将沿用此模式。
- 设置新增"小数位数"选项（3–6 位），控制所有数值显示的小数精度。

## v0.2.0

- 引入物理资源体系：频率 F = 波速 U / 波长 L，默认采用国际单位制。
- 波动页面新增"主要"与"升级"两个子页面。
- 主要页：三个可重复购买升级（增加基础波速获取 / 加成波速获取 / 缩短波长重置波速），升级 3 在 F ≥ 50000 Hz 时显现。
- 升级页：单次升级"频率加成波长获取"，效果 (log₁₀(F+1)+1)^1.5。
- 统计页新增历史最高频率、最高波速、最低波长、成就完成数。
- 设置页新增数值显示方式（科学 / 工程 / 对数）。
- 新增成就标签页：普通成就（5 列网格，按行解锁）与隐藏成就（点击揭示）。
- 修复升级按钮与成就单元格因每 tick 重建 DOM 导致的闪烁/点击丢失（改为 build-once + 原地更新）。

## v0.1.0

- 初始壳子：波动 / 统计 / 设置 / 帮助 四个页面。
- 基础资源"频率"10 Hz 起步。
- 存档导入导出（base64）、存档槽、硬重置、黑白主题切换。
